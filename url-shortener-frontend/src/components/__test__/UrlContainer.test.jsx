import UrlContainer from "../UrlContainer";


// Test the urlContainer component
describe('Test the url component', () => {
  test('render the form with one button', () => {
    render(<UrlContainer />);
  })
  
})
